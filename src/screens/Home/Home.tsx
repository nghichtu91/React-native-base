import React from 'react';
import { ScreenWraper } from '@Components/ScreenWraper';
import { Text, View } from 'react-native';

export const Home = () => {
  return (
    <ScreenWraper>
      <View>
        <Text> GetStrated Screen </Text>
      </View>
    </ScreenWraper>
  );
};
